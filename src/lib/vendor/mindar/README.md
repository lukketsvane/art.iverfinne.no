Vendored from mind-ar@1.2.5 (MIT, see LICENSE) — browser bundles only.
The npm package's install script needs native canvas (fails in CI), so the
prebuilt dist files live here instead. One patch applied for three >= r152:
sRGBEncoding/outputEncoding -> SRGBColorSpace/outputColorSpace.
